Инфраструктура для проекта разворачивается в Yandex Cloud скриптом `start.sh` в папке **infra**.
Сервисы проекта запускаются командой `docker stack deploy -c compose.yaml`.
Скриншоты находятся в папке **artifacts**.
